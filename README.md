# Deno unzip script for termux

script for termux using deno to unzip .zip .rar and .7z files

---
### downloading
requirements are `Deno` and `7z` for it use `pkg install deno 7z`

next clone the repo then run `deno task swapEditor` to place the bundled script in ~/bin/`


